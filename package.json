{
  "scripts": {
    "generateNoMin": "node src/index.js",
    "generate": "node src/index.js && html-minifier --input-dir ./dist --output-dir ./dist-out  --collapse-whitespace --remove-comments --remove-attribute-quotes && rm -r dist && mv dist-out dist",
    "dev": "http-server ./dist"
  },
  "dependencies": {
    "html-minifier": "^4.0.0",
    "http-server": "^14.1.1",
    "nunjucks": "^3.2.3"
  }
}
